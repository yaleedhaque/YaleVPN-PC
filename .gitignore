# YaleVPN-PC
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# local state, never commit
state/
*.log

# credentials / secrets — kept in the gpg vault, not here
*.gpg
credentials*
.env
secrets*

# contains real WARP private key — never commit
config/

# OS junk
.DS_Store
Thumbs.db

# editor
.idea/
.vscode/